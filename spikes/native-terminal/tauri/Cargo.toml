[package]
name = "native-terminal-tauri-spike"
version = "0.1.0"
description = "Tauri/AppKit native terminal composition spike"
edition = "2021"

[build-dependencies]
cc = "1"
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
tauri = { version = "2", features = [] }
